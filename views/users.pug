extends layouts/main-layout.pug

block content    
    section 
        ul 
        if users.length>0
            each user of users 
                li #{user.name}
        else 
            li No Users Present
